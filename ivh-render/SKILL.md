---
name: ivh-render
description: 信息图视频流水线第 4 环——渲染。把单文件 HTML 产物渲染成视频：独立成片输出 H.264 MP4，透明素材输出带 alpha 的 ProRes 4444 .mov，并自动复检透明通道真伪。当用户说"导出视频""渲染成 mp4""把这个 HTML 录成视频""要透明通道的 mov""出片"时使用。
agent_created: true
---

# 渲染（信息图视频 · 第 4 环）

**只干一件事：HTML → 视频。不改内容、不改样式、不碰脚本。**

```
ivh-standalone ─┐
                ├─→ 产物.html ──→ [ 你在这里 ] ──→ .mp4 / .mov
ivh-overlay  ───┘
```

输出格式**由产物自己的 `CONFIG.BG` 决定**，不由命令行指定——
选错了不会报错，只会安静地产出一份没法用的文件。

---

## 一、跑一条命令就够

```bash
node scripts/render.mjs 产物.html
```

它按顺序做三件事：

1. **前置检查** —— 读产物 CONFIG，把「出片必然出错」的组合挡下来（未通过直接中止）
2. **抽帧 + 合成** —— 委托 `shoot.mjs`，透明走 ProRes 4444，不透明走 H.264
3. **透明复检** —— 透明产物自动跑 `check-alpha.mjs` 抽 alpha 平面，不合格就报错

常用选项：

```bash
node scripts/render.mjs 产物.html --preset draft     # 0.5s 一帧，快速看节奏
node scripts/render.mjs 产物.html --preset fine      # 0.04s 一帧，交付级
node scripts/render.mjs 产物.html --preflight        # 只检查，不渲染
node scripts/render.mjs 产物.html --out ./dist
node scripts/render.mjs 产物.html --scale 2          # 2 倍分辨率
```

## 二、关于「检查」：这一环没有抽帧自查

出片是流水线的终点。**进来之前，用户已经看过 HTML 并说没问题了**——
"画面好不好看"的判断在 ②③ 环就做完了，这里不再重复一遍。

`render.mjs` 里三个带"检查"字样的步骤，性质完全不同：

| 步骤 | 拍不拍图 | 成本 | 判什么 |
|---|---|---|---|
| ① 前置检查 | **不拍** —— 纯读 HTML 源码的正则 | 毫秒 | 出片必然失败的组合（时长算不出、透明没藏 HUD） |
| ② 抽帧合成 | **拍** —— 但这**就是出片本身** | 分钟级 | 不判任何东西，产出的就是成品 |
| ③ 透明复检 | 读已产出的 `.mov`，抽 12 个时间点 | 秒级 | 文件能不能用（假 alpha 是头号事故） |

**② 躲不掉。** 这套链路是「按秒定位 → 截图 → 串成序列 → 合成」——
截图不是检查手段，是生产方式本身。想快只能抽稀一点，不能取消。

**③ 只判"文件能不能用"，不判"好不好看"**，而且只读已经产出的文件、不再动浏览器。
透明素材不要跳过它。

> 判断一个步骤该不该留，就看它判的是**文本/文件**还是**观感**：
> 前者交给机器（快、准、可重复），后者交给人（一次就够）。
> 让 AI 反复抽帧看画面，等于把一个人的活干成了一台慢机器的事。

## 三、前置检查会拦下什么

| 检查项 | 拦下原因 |
|---|---|
| 总时长必须 > 0 | `timeline` 取 `max(data-out)`；`text` 累加 `data-sec`。算不出来就没有帧序列 |
| `timeline` 必须有 `.comp` | 没有组件就没有时间序列 |
| `text` 必须有 `.scene` | 没有幕就没有内容 |
| `timeline` 的组件必须有 `data-in`/`data-out` | 缺了它们组件不会上场 |
| 透明产物必须 `HIDE_HUD: true` | 否则 `#hud` / `#kbd` / `#bar` 会被渲染进素材，底部留一条彩色进度条 |
| `ANIMATE` 建议为 `true` | 否则出片只有布局、没有动画（警告，不阻断） |

前三条任一不过，**中止出片**。这不是保守——它们是"产出了但没法用"的典型，
越晚发现越浪费。

> **两条排版路径都能出片。** `text`（分幕）同样是时间序列：模板把全局秒数换算成
> 「第 k 幕 + 幕内偏移」，用负 `animation-delay` + 暂停定格，抽帧走 `?t=`。
> 不带 `--step` 时才退回「每幕一张」的预览模式（走 `#<幕号>`）。

## 四、抽帧密度：唯一的取舍点

这套链路是「按秒定位 → 截图 → 串序列 → 合成」，**每帧都要拉起一次浏览器**，
所以帧数直接等于耗时。

| preset | `--step` | 30s 片子 | 适合 |
|---|---|---|---|
| `draft` | 0.5s | ~61 帧 | 只用于用户明确要求的快速试片 |
| `normal`（默认） | 0.1s | ~301 帧 | 日常出片，入场动能看出过程 |
| `fine` | 0.04s | ~751 帧 | 交付级，很慢 |

**默认一把出 `normal`，出完就交。**

不要养成"先出个 draft 看一眼、再重出正式版"的习惯——出片是分钟级的，
跑两轮就是双倍时间，而**内容的对错在用户看 HTML 那一关已经确认过了**。

`draft` 只在用户明确说"先出个粗的看节奏"时才用。

> **不要用 `--fps` 补帧。** 它只是重排已有帧，补出来的是重复帧，文件涨几十倍而画面毫无变化。
> 要更流畅只能降低 `--step`——采更多**真实**帧。

## 五、透明：必须验到底层像素

`ffprobe` 报 `pix_fmt=yuva444p10le` **只说明有 alpha 平面，不说明平面里有内容**。
假 alpha（全 255）同样是 `yuva444p10le`，叠到实拍上就是一块实心白板。

**唯一可信判据是抽 alpha 平面看分布**（`check-alpha.mjs`）。`render.mjs` 已自动跑，
不要随手加 `--no-verify`。

`check-alpha.mjs` **按时间点抽样，不解全片**（默认 12 个点）：
alpha 平面转 rawvideo 是 1.48MB/帧（1080x1440），一条 30s 素材全解就是 1.1GB——
塞进进程缓冲直接爆，还会被误读成"透明不合格"。抽样后是秒级，判据一点没变，
而且会单独报出**最差的一帧**（整体达标但某一帧是实心，叠上去就是一闪的白板）。

```bash
node scripts/check-alpha.mjs 产物-alpha.mov --samples 20   # 想抽得更密
```

## 六、为什么不用实时录屏

常规做法是 CDP screencast。**本机沙箱走不通**：沙箱隔离了子进程网络，
Node `spawn` 的浏览器活着，但 Node 回连不上 `--remote-debugging-port`（`ETIMEDOUT`）。

所以一律走浏览器自带 CLI：`--screenshot=<path>` 落盘、`--dump-dom` 读回状态，
**全程零网络**。`--default-background-color=00000000` 是透明能否成立的开关，
已内置在 `shoot.mjs` 里，**不要去掉**。

## 七、文件

| 路径 | 用途 |
|---|---|
| `scripts/render.mjs` | **主入口**：前置检查 → 出片 → 透明复检 |
| `scripts/shoot.mjs` | 抽帧 / 逐幕截图 / 六风格对照 / `--video` 合成 |
| `scripts/shoot-at.mjs` | 定向复核：按组件入出点采样，核对卡点是否对齐 SRT |
| `scripts/check-alpha.mjs` | 抽 alpha 平面（按时间点抽样），判真透明 |
| `references/render-matrix.md` | 出片矩阵、抽帧取舍、故障排查表 |
| `references/interop.md` | **产物契约**：渲染器靠哪些属性读数 |

## 八、边界

- ❌ 不修改产物内容 —— 发现问题回到 ②③ 改，不要在渲染层打补丁
- ❌ 不改画布尺寸 —— 产物由 `data-width` / `data-height` 自报，命令行只能 `--scale` 倍率
- ❌ 不替产物决定格式 —— 一切都看 `CONFIG.BG`
- ❌ 不做画面质量判断 —— 那是用户看 HTML 时的事（见第二节）

**前置检查报的错，正确做法是回上游改产物，不是在这里绕过去。**
